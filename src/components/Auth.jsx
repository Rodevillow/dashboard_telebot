import React from "react";

import "../assets/scss/pages/signIn.scss";

const Auth = (props) => {
    return (
        <div>
             Login component
        </div>
    );
};

export default Auth;
